%h1 Listing polls

%ul
  - @polls.each do |poll|
    %li
      = link_to poll.title, poll
      
      .div{:class => "btn-group"}
        = link_to 'Edit', edit_poll_path(poll), class: "btn btn-default" if poll.user == current_user
        = link_to 'Destroy', poll, :method => :delete, :data => { :confirm => 'Are you sure?' }, class: "btn btn-default" if poll.user == current_user

%p= link_to 'New Poll', new_poll_path, class: "btn btn-primary"
